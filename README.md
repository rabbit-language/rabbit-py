# rabbit
Rabbit: A concise, elegant programming language combining Python-like simplicity with static typing. Features colon-indent syntax, dual JIT/AOT compilation, and built-in concurrency. Write `Int x = 10` or `x = 10`, define functions with `def`, and run anywhere with Python-speed development or C-speed performance.
